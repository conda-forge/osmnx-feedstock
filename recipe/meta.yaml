{% set version = "0.2" %}

package:
  name: osmnx
  version: {{ version }}

source:
  fn: osmnx-{{ version }}.tar.gz
  url: https://github.com/gboeing/osmnx/archive/v{{ version }}.tar.gz
  sha256: 6a8eb7ee64e28c24c531e09c8a54a794edbdc949879ced72e108537223a493ca

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
  run:
    - python
    - requests
    - numpy
    - pandas >=0.19
    - geopandas >=0.2
    - networkx
    - matplotlib
    - shapely
    - descartes
    - geopy >=1.11
    - rtree >=0.8.3

test:
  imports:
    - osmnx

about:
  home: https://github.com/gboeing/osmnx
  license: MIT
  license_file: LICENSE.txt
  summary: 'Retrieve, construct, analyze, and visualize street networks from OpenStreetMap'
  license_family: MIT

extra:
  recipe-maintainers:
    - ocefpaf
    - gboeing
